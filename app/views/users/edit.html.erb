<%= simple_form_for [@user] do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :address %>
  <%= f.input :bio %>
  <%= f.submit "Save changes", class: "btn btn-primary" %>
<% end %>
